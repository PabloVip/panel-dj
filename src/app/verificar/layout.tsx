import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Verificación",
};

export default function LayoutVerificar(props: { children: React.ReactNode }) {
  return props.children;
}
